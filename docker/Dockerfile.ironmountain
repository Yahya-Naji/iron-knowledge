FROM python:3.11-slim

WORKDIR /workspace

# Install minimal dependencies for Iron Mountain MCP servers and cancellation service
RUN pip install --no-cache-dir \
    mcp \
    psycopg2-binary \
    fastapi \
    uvicorn

# Keep container running by default
CMD ["tail", "-f", "/dev/null"]

